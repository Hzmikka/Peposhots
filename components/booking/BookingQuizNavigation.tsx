import { ArrowLeft,ArrowRight,Send } from "lucide-react";import styles from "./BookingQuiz.module.css";
export function BookingQuizNavigation({step,total,valid,loading,onBack,onNext}:{step:number;total:number;valid:boolean;loading:boolean;onBack:()=>void;onNext:()=>void}){return <footer className={styles.navigation}>{step>0?<button className={styles.back} type="button" onClick={onBack}><ArrowLeft size={17}/>Atrás</button>:<span/>}<button className={styles.next} type="button" disabled={!valid||loading} onClick={onNext}>{step===total-1?(loading?"Enviando consulta…":<>Enviar consulta <Send size={17}/></>):<>Continuar <ArrowRight size={17}/></>}</button></footer>}
