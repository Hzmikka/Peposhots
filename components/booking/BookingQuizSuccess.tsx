import{CircleCheck,MessageSquare,Phone}from"lucide-react";import{createSmsLink,phoneLink}from"@/lib/contactLinks";import styles from"./BookingQuiz.module.css";
export function BookingQuizSuccess({data,id,onClose,onReset}:{data:any;id:string;onClose:()=>void;onReset:()=>void}){return <div className={styles.success}><CircleCheck size={44}/><h2>Recibimos los detalles de tu evento</h2><p>PepoShots revisará la fecha, la ubicación y el tipo de servicio antes de confirmar disponibilidad contigo.</p><div className={styles.receipt}><span>Consulta <b>{id.slice(0,8).toUpperCase()}</b></span><span>Fecha <b>{data.dateStatus==="unknown"?"Fecha por definir":data.eventDate}</b></span><span>Evento <b>{data.customEventType||data.eventType}</b></span><span>Zona <b>{data.zone}</b></span></div><strong>Esta consulta no confirma automáticamente la reserva.</strong><div className={styles.successButtons}><a href={createSmsLink(data)}><MessageSquare size={16}/>Hablar por texto</a><a href={phoneLink}><Phone size={16}/>Llamar ahora</a><button onClick={onClose}>Cerrar</button><button onClick={onReset}>Enviar otra consulta</button></div></div>}
