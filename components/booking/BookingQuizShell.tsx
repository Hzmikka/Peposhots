import Image from "next/image";import {X} from "lucide-react";import type{ReactNode}from"react";import {BookingQuizProgress}from"./BookingQuizProgress";import styles from"./BookingQuiz.module.css";
export function BookingQuizShell({step,total,onClose,children,navigation}:{step:number;total:number;onClose:()=>void;children:ReactNode;navigation:ReactNode}){return <div className={styles.overlay}><section className={styles.shell} role="dialog" aria-modal="true" aria-label="Consulta tu evento"><header className={styles.quizHeader}><Image src="/logo/isotipo.png" alt="" width={38} height={38}/><div><b>Consulta tu evento</b><BookingQuizProgress step={step} total={total}/></div><button type="button" onClick={onClose} aria-label="Cerrar consulta"><X/></button></header><main className={styles.content}>{children}</main><p className={styles.permanent}>Tu fecha no queda confirmada automáticamente. PepoShots revisará los detalles y se comunicará contigo.</p>{navigation}</section></div>}
